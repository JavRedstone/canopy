// Shared accent colors used across the site for anything tied to the mastery model's two
// tracks -- conceptual understanding vs. applied skill -- plus a third for assessments.
// Indigo and teal are adjacent hues, so gradients between them stay smooth; avoid pairing
// either with a near-complementary color (e.g. orange), which muddies at the blend midpoint.
export const UNDERSTAND_COLOR = "#818cf8";
export const APPLY_COLOR = "#2dd4bf";
export const ASSESSMENT_COLOR = "#fbbf24";

// The canopy green from public/canopy-logo.svg. For brand moments -- in-progress work, the
// growing-tree loader -- rather than the mastery tracks above, which carry their own meaning.
export const CANOPY_GREEN = "#6aa84f";
